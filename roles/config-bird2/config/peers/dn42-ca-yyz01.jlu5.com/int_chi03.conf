protocol bgp ibgp_chi03 {
    local as OWNAS;
    neighbor fd86:bad:11b7:20::1 as OWNAS;
    rr client;
    passive on;

    ipv4 {
        import none;
        export where ibgp_export_filter();
    };
    ipv6 {
        import none;
        export where ibgp_export_filter();
    };
}
