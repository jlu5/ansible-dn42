- name: Configure dn42 routers
  hosts: dn42routers
  become: yes
  # Linear operation makes templating peer data really slow...
  strategy: free
  roles:
    - config-loopback
    - config-wireguard
    - config-bird2
